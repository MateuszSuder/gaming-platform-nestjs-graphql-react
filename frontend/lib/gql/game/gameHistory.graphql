query gameHistory($historyInput: GameHistoryDto!) {
    gameHistory(historyInput: $historyInput) {
        history {
            bet
            createdAt
            game
            multiplier
            win
        }
    }
}